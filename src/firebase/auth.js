import { html, render } from 'lit-html';

export class AUTH {
    constructor() {
        // this.firebase = firebase;
        this.user;


    }

    start() {
        let self = this;
        
    }

    setUser(user) {
        
    }

    handleLoggedIn(user) {
        this.setUser(user);
        this.renderMainScreenControls();
    }

    renderMainScreenControls() {
        
    }
}